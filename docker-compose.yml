name: esper

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?set POSTGRES_USER in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:?set POSTGRES_DB in .env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    # Unbounded json-file logs are the classic way a home server runs out of disk.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    # PostgreSQL is only reachable from the internal Docker network.
    # Uncomment to expose it on the host (e.g. for a DB client):
    # ports:
    #   - "127.0.0.1:5432:5432"

  migrate:
    build:
      context: .
      target: migrate
      args:
        NEXT_PUBLIC_APP_NAME: ${NEXT_PUBLIC_APP_NAME:-Esper}
        NEXT_PUBLIC_THEME_COLOR: ${NEXT_PUBLIC_THEME_COLOR:-#0f766e}
    env_file: ${ENV_FILE:-.env}
    environment:
      NODE_ENV: production
      # "Today" / "this month" are computed in this zone.
      TZ: ${TZ:-Asia/Kolkata}
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    # Unbounded json-file logs are the classic way a home server runs out of disk.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

  app:
    build:
      context: .
      target: runner
      args:
        NEXT_PUBLIC_APP_NAME: ${NEXT_PUBLIC_APP_NAME:-Esper}
        NEXT_PUBLIC_THEME_COLOR: ${NEXT_PUBLIC_THEME_COLOR:-#0f766e}
    restart: unless-stopped
    env_file: ${ENV_FILE:-.env}
    environment:
      NODE_ENV: production
      # "Today" / "this month" are computed in this zone.
      TZ: ${TZ:-Asia/Kolkata}
      PORT: "3000"
      HOSTNAME: 0.0.0.0
    ports:
      - "${APP_PORT:-3000}:3000"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    # The image defines its own HEALTHCHECK (see Dockerfile); restated here so the
    # intent is visible where the service is configured.
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/api/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    # Unbounded json-file logs are the classic way a home server runs out of disk.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  postgres_data:
